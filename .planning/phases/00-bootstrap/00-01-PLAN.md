---
phase: 00-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - flake.nix
  - flake.lock
  - .envrc
  - src/proxy.py
  - src/filter.py
  - src/logger.py
  - src/monitor.py
  - src/cache.py
  - src/config.py
  - config/blocked_domains.txt
  - logs/.gitkeep
  - README.txt
autonomous: true
requirements: [REQ-00]

must_haves:
  truths:
    - "direnv allow activa entorno Nix con Python 3"
    - "python --version funciona dentro del shell"
    - "Estructura src/ config/ logs/ existe con archivos placeholder"
    - "Commit inicial con todos los archivos de bootstrap"
  artifacts:
    - path: "flake.nix"
      provides: "Nix dev shell with Python 3 + dependencies"
      min_lines: 20
    - path: ".envrc"
      provides: "Direnv activation via use flake"
      contains: "use flake"
    - path: ".gitignore"
      provides: "Project-specific ignore rules"
      contains: "logs/*.log"
    - path: "src/proxy.py"
      provides: "Main proxy entry point placeholder"
    - path: "src/config.py"
      provides: "Configuration module placeholder"
    - path: "config/blocked_domains.txt"
      provides: "Domain blocklist placeholder"
    - path: "README.txt"
      provides: "Project documentation"
      min_lines: 10
  key_links:
    - from: ".envrc"
      to: "flake.nix"
      via: "direnv use flake references flake.nix"
      pattern: "use flake"
---

<objective>
Bootstrap Nix flake dev environment for HTTP proxy project.

Purpose: Establish reproducible development environment and project structure as foundation for all subsequent phases.
Output: Nix flake, direnv config, gitignore, directory structure, placeholder source files, README.
</objective>

<execution_context>
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-bootstrap/00-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Update .gitignore for project needs</name>
  <files>.gitignore</files>
  <read_first>.gitignore</read_first>
  <action>
    Add project-specific entries at end of file:
    - `logs/*.log` — exclude log output
    - `.direnv/` — exclude direnv cache
    - `result` — exclude Nix build symlink

    Remove `.envrc` from the "Environments" section (line ~152) — .envrc must be tracked in git for this project.
  </action>
  <verify>grep -q 'logs/*.log' .gitignore && grep -q '.direnv/' .gitignore && grep -q '^result$' .gitignore && ! grep -q '^.envrc$' .gitignore</verify>
  <acceptance_criteria>
    - .gitignore contains "logs/*.log"
    - .gitignore contains ".direnv/"
    - .gitignore contains "result"
    - .gitignore does NOT contain ".envrc" (removed)
  </acceptance_criteria>
  <done>.gitignore updated with project entries</done>
</task>

<task type="auto">
  <name>Create flake.nix</name>
  <files>flake.nix</files>
  <read_first>.gitignore</read_first>
  <action>
    Create flake.nix with:
    - inputs: nixpkgs.url = "github:NixOS/nixpkgs/nixos-unstable"
    - outputs.devShells.default with:
      - buildInputs: python3, python3Packages.requests, python3Packages.flask, git, curl, wireshark
      - shellHook: echo "HTTP Proxy Dev Environment ready"
    - Use `with import` pattern or `pkgs.mkShell` — standard Nix flake format
  </action>
  <verify>nix flake check 2>&1 || echo "flake check skipped (may need nix command)"</verify>
  <acceptance_criteria>
    - flake.nix exists with nixpkgs input
    - flake.nix contains "devShells.default"
    - flake.nix contains "python3Packages.requests"
    - flake.nix contains "python3Packages.flask"
    - flake.nix contains "HTTP Proxy Dev Environment ready"
  </acceptance_criteria>
  <done>flake.nix created</done>
</task>

<task type="auto">
  <name>Create .envrc</name>
  <files>.envrc</files>
  <read_first>flake.nix</read_first>
  <action>
    Create .envrc with single line: `use flake`
    This enables direnv to automatically activate the Nix flake dev shell when entering the directory.
  </action>
  <verify>cat .envrc | grep -q 'use flake'</verify>
  <acceptance_criteria>
    - .envrc exists with content "use flake"
  </acceptance_criteria>
  <done>.envrc created</done>
</task>

<task type="auto">
  <name>Create directory structure and placeholders</name>
  <files>
    src/proxy.py
    src/filter.py
    src/logger.py
    src/monitor.py
    src/cache.py
    src/config.py
    config/blocked_domains.txt
    logs/.gitkeep
  </files>
  <read_first>.gitignore</read_first>
  <action>
    Create directories src/, config/, logs/ if not exist.

    Create placeholder Python files in src/ — each file gets a module docstring and a comment indicating its purpose:
    - proxy.py: "HTTP Proxy — entry point and connection handling"
    - filter.py: "Domain and keyword filtering"
    - logger.py: "Request logging to file"
    - monitor.py: "Flask monitoring dashboard"
    - cache.py: "In-memory HTTP response cache"
    - config.py: "Configuration constants"

    Create config/blocked_domains.txt with test domains:
    ```
    # Dominios bloqueados
    facebook.com
    twitter.com
    ads.google.com
    ```

    Create logs/.gitkeep (empty file to track empty directory).
  </action>
  <verify>test -f src/proxy.py && test -f src/config.py && test -f config/blocked_domains.txt && test -f logs/.gitkeep</verify>
  <acceptance_criteria>
    - src/proxy.py exists with module docstring
    - src/filter.py exists
    - src/logger.py exists
    - src/monitor.py exists
    - src/cache.py exists
    - src/config.py exists
    - config/blocked_domains.txt contains test domains
    - logs/.gitkeep exists (empty)
  </acceptance_criteria>
  <done>Placeholder files created</done>
</task>

<task type="auto">
  <name>Create README.txt</name>
  <files>README.txt</files>
  <read_first>.gitignore, flake.nix, .envrc</read_first>
  <action>
    Create README.txt with basic project info:
    ```
    HTTP Proxy con Filtrado y Monitoreo
    EIF208 - Redes, Universidad Nacional (UNA)
    Junio 2026

    === Entorno de Desarrollo ===

    1. Activar entorno:
       direnv allow
       (o: nix develop)

    2. Ejecutar proxy:
       python src/proxy.py

    === Configuración del Navegador ===

    Firefox: Preferences → Network → Manual proxy → HTTP: localhost 8080

    === Estructura del Proyecto ===

    src/          Código fuente Python
    config/       Archivos de configuración
    logs/         Archivos de log generados
    ```
  </action>
  <verify>test -s README.txt</verify>
  <acceptance_criteria>
    - README.txt exists with at least 10 lines
    - README.txt contains "direnv allow"
    - README.txt contains "python src/proxy.py"
  </acceptance_criteria>
  <done>README.txt created</done>
</task>

<task type="auto">
  <name>Initial commit</name>
  <files>.</files>
  <read_first>.gitignore</read_first>
  <action>
    Stage all created files and commit with message: "chore: bootstrap nix flake dev environment with direnv"
    Files to stage: flake.nix flake.lock .envrc .gitignore src/ config/ logs/ README.txt .planning/
    Don't forget flake.lock (auto-generated on first nix invocation, or skip if nix not available).
  </action>
  <verify>git log --oneline -1 | grep -q "bootstrap nix flake"</verify>
  <acceptance_criteria>
    - git log shows commit with message "chore: bootstrap nix flake dev environment with direnv"
    - All project files tracked in the commit
  </acceptance_criteria>
  <done>Initial commit created</done>
</task>

</tasks>

<verification>
- [ ] flake.nix exists and passes nix flake check
- [ ] .envrc exists with use flake
- [ ] .gitignore has project entries (logs/*.log, .direnv/, result)
- [ ] src/*.py placeholders exist
- [ ] config/blocked_domains.txt has test domains
- [ ] README.txt has basic instructions
- [ ] git log shows bootstrap commit
</verification>

<success_criteria>

- All tasks completed
- Environment ready for Phase 1 development
- Reproducible via `direnv allow` or `nix develop`
</success_criteria>

<artifacts>
## Artifacts this phase produces

### New files
- `flake.nix` — Nix flake dev shell definition
- `flake.lock` — Nix flake lockfile (reproducibility)
- `.envrc` — Direnv activation
- `README.txt` — Project documentation
- `src/proxy.py` — Main proxy entry point (placeholder)
- `src/filter.py` — Domain filtering module (placeholder)
- `src/logger.py` — Request logging module (placeholder)
- `src/monitor.py` — Monitoring dashboard module (placeholder)
- `src/cache.py` — HTTP cache module (placeholder)
- `src/config.py` — Configuration constants module (placeholder)
- `config/blocked_domains.txt` — Domain blocklist with test entries
- `logs/.gitkeep` — Empty log directory placeholder

### Modified files
- `.gitignore` — Updated with project-specific ignore rules

### Commits
- `chore: bootstrap nix flake dev environment with direnv`
</artifacts>

<output>
After completion, create `.planning/phases/00-bootstrap/00-01-SUMMARY.md`
</output>
